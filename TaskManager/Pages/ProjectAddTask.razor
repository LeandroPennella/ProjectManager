@using TaskManager.Data.Models
@inject TaskManager.Data.TaskManagerDbContext Db
@code {
    private string newTaskTitle = string.Empty;
    
    private void AddTask()
    {
        if (!string.IsNullOrWhiteSpace(newTaskTitle))
        {
            var task = new TaskItem
            {
                Title = newTaskTitle,
                //SectionId = SectionId // Assuming SectionId is defined in the parent component
            };
            Db.Tasks.Add(task);
            Db.SaveChanges();
            newTaskTitle = string.Empty;
        }
    }
}


    <li class="list-group-item d-flex flex-column">
        <div class="d-flex justify-content-between align-items-center">
            <div>
            <input @bind="newTaskTitle" placeholder="Título de la tarea" class="form-control form-control-sm mb-1" />
            </div>
            <button class="btn btn-primary btn-sm" @onclick="AddTask">Agregar</button>
        </div>
    </li>

